public class ConfigParser {
    // TODO: this class parse the file node.conf
    // If the uuid option does not exist, you must generate a new UUID (see 3.2) and update the configuration file (either node.conf or those specified in –c parameter) with the generated identifier.
    
}
